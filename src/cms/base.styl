// Remove extra space under contest iframe
iframe[src*="digitalivy.com"] {
	display: inherit;
}

.themify_builder_turn_on {
	display: none !important;
}

body .wrapper-content .CMLS_CCC {
	#comments,
	article > header,
	article > footer,
	.entry-title,
	.entry-meta,
	.post-thumbnail,
	.hideme {
		display: none !important;
	}
	.entry-content {
		margin-top: 0 !important;
	}
}